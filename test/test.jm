class Pikachu {

    public static void main(string[] state) {
    if(<>(health < 2)) {
    health = health - 2;
    }
    print("new health");
    }
}
class x {
    number health;
    final number maxMovesNum = 5;
    private number speed;
    boolean legendary;
    Pokeball ball;
    Move[] moves = new Moves[maxMovesNum];
    private final number x;
    public void attack(Pokemon target, number amount) {
    target.damage(amount);
    array = new number[66];
    print(array.length);
    }
}
interface Pokemon {
    void speedUp(number a);
}

class Pikachu implements Pokemon {
    @Override
    public number damage() {
    if(<>(health < 2)) {
    health = health - 2;
    }
    number i;
    while (i < 5) {
      print(i);
      i = i + 1;
    }
    print("new health");
    ret health;
    }
}